// Module Start
// JS imports
import Container from '@material-ui/core/Container';

// Layout
const Layout = (props) => (
  /* Container Start */
  <Container>
    {props.children}
  </Container>
  /* Container End */
);

// Module export
export default Layout;
// Module End
