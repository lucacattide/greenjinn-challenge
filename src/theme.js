// Module Start
// JS Imports
import { createMuiTheme } from '@material-ui/core/styles';

// Theme
const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#62af6f',
      main: '#317f43',
      dark: '#00521a',
      contrastText: '#fff'
    },
    secondary: {
      light: '#ffff52',
      main: '#fad201',
      dark: '#c2a100',
      contrastText: '#212121'
    }
  }
});

// Module export
export default theme;
// Module End
